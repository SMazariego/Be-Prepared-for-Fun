<article class="vacay">
  <div class="title">
    <a href="vacay/{{id}}">{{title}}</a>
    {{!-- <span>({{format_url post_url}})</span> --}}
  </div>
  <div class="meta">
    by
    {{user.username}}
    on
    {{format_date created_at}}
    |
    {{! update to info we want displayed for each vacation  }}
    <a href="vacay/{{id}}">
      Agenda</a>
      {{!-- ^ need to fix  --}}
  </div>
</article>